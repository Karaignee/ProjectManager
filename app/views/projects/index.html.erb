<h1>All Projects</h1>

<%= render 'table' %>

<%= form_tag projects_path, :method => :get do %>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search by location", :name => nil, :class => 'btn btn-primary' %>
    </p>
<% end %>


<br />

<%= link_to 'New Project', new_project_path %>
